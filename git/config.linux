# Credential helper
[credential]
	helper = libsecret
[diff]
	guitool = kdiff3
[merge]
	guitool = kdiff3
	tool = vim_mergetool
[mergetool "vim_mergetool"]
	cmd = nvim -f -c "MergetoolStart" "$MERGED" "$BASE" "$LOCAL" "$REMOTE"
	trustExitCode = true
